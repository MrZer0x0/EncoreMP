diff --git a/.github/workflows/windows.yml b/.github/workflows/windows.yml
index 12012af..fdaf515 100644
--- a/.github/workflows/windows.yml
+++ b/.github/workflows/windows.yml
@@ -33,9 +33,9 @@ jobs:
         uses: actions/cache@v4
         with:
           path: deps
-          key: win-deps-v3-${{ hashFiles('CI/before_script.msvc.sh') }}
+          key: win-deps-v4-${{ hashFiles('CI/before_script.msvc.sh', '.github/workflows/windows.yml') }}
           restore-keys: |
-            win-deps-v3-
+            win-deps-v4-
 
       # ── Скачиваем CrabNet (замена RakNet) ──
       - name: Download CrabNet
@@ -44,28 +44,12 @@ jobs:
             https://github.com/DreamWeave-MP/CrabNet/releases/download/Stable-CI/CrabNet-Windows-Release.7z
           7z x -y CrabNet.7z -oextern/raknet
 
-      # ── Фикс: aqtinstall 1.1.3 несовместим с setuptools>=82 ──
-      # setuptools 82+ удалил pkg_resources. Обновляем aqt до v3 и правим синтаксис.
-      - name: Fix aqtinstall compatibility
-        run: |
-          # 1) Обновляем версию aqtinstall
-          sed -i 's/aqtinstall==1.1.3/aqtinstall>=3.1.0/g' CI/before_script.msvc.sh
-
-          # 2) Исправляем команду aqt install → aqt install-qt с правильным порядком аргументов
-          #    Старый формат (v1): aqt install <version> windows desktop "<arch>"
-          #    Новый формат (v3):  aqt install-qt windows desktop <version> <arch>
-          sed -i 's|aqt" install ${QT_VER} windows desktop "win${BITS}_msvc${QT_MSVC_YEAR}${SUFFIX}"|aqt" install-qt windows desktop ${QT_VER} win${BITS}_msvc${QT_MSVC_YEAR}${SUFFIX}|' CI/before_script.msvc.sh
-
-          # Проверяем результат
-          echo "=== Patched aqt line ==="
-          grep -n "aqt.*install-qt\|aqtinstall" CI/before_script.msvc.sh
-
       # ── Запуск штатного скрипта подготовки зависимостей MSVC ──
       - name: Setup Dependencies & Configure
         run: |
-          CI/before_script.msvc.sh \
+          bash CI/before_script.msvc.sh \
             -c ${{ env.BUILD_TYPE }} \
-            -p x64 \
+            -p Win64 \
             -v 2022 \
             -V \
             -i "${{ github.workspace }}/install"
@@ -73,14 +57,12 @@ jobs:
       # ── Сборка ──
       - name: Build
         run: |
-          cd MSVC2022_64
-          cmake --build . --config ${{ env.BUILD_TYPE }} -- //m:$(nproc)
+          cmake --build MSVC2022_64 --config ${{ env.BUILD_TYPE }} --parallel
 
       # ── Установка ──
       - name: Install
         run: |
-          cd MSVC2022_64
-          cmake --install . --config ${{ env.BUILD_TYPE }}
+          cmake --install MSVC2022_64 --config ${{ env.BUILD_TYPE }}
 
       # ── Упаковка артефакта ──
       - name: Package
diff --git a/CI/before_script.msvc.sh b/CI/before_script.msvc.sh
index e8020cd..d800423 100644
--- a/CI/before_script.msvc.sh
+++ b/CI/before_script.msvc.sh
@@ -891,7 +891,7 @@ printf "Qt ${QT_VER}... "
 
 	QT_SDK="$(real_pwd)/Qt/${QT_VER}/msvc${QT_MSVC_YEAR}${SUFFIX}"
 
-		if [ -d "Qt/${QT_VER}" ]; then
+		if [ -d "$QT_SDK" ]; then
 		printf "Exists. "
 	elif [ -z $SKIP_EXTRACT ]; then
 		if [ $MISSINGPYTHON -ne 0 ]; then
@@ -913,16 +913,11 @@ printf "Qt ${QT_VER}... "
 			wrappedExit 1
 		fi
 
-				# Fix missing setuptools and pkg_resources
-		echo "  Ensuring setuptools is installed in virtualenv..."
+				echo "  Ensuring pip/setuptools/wheel are installed in virtualenv..."
 		run_cmd "aqt-venv/${VENV_BIN_DIR}/python" -m pip install --upgrade pip setuptools wheel
 
-		# Check version
-		aqt-venv/${VENV_BIN_DIR}/pip list | grep 'aqtinstall\s*1.1.3' || [ $? -ne 0 ]
-		if [ $? -eq 0 ]; then
-			echo "  Installing aqt wheel into virtualenv..."
-			run_cmd "aqt-venv/${VENV_BIN_DIR}/pip" install aqtinstall==1.1.3
-		fi
+		echo "  Installing/upgrading aqtinstall in virtualenv..."
+		run_cmd "aqt-venv/${VENV_BIN_DIR}/python" -m pip install --upgrade aqtinstall
 		popd > /dev/null
 
 		rm -rf Qt
@@ -930,10 +925,10 @@ printf "Qt ${QT_VER}... "
 		mkdir Qt
 		cd Qt
 
-		run_cmd "${DEPS}/aqt-venv/${VENV_BIN_DIR}/aqt" install ${QT_VER} windows desktop "win${BITS}_msvc${QT_MSVC_YEAR}${SUFFIX}"
+		run_cmd "${DEPS}/aqt-venv/${VENV_BIN_DIR}/aqt" install-qt windows desktop ${QT_VER} "win${BITS}_msvc${QT_MSVC_YEAR}${SUFFIX}"
 
 		printf "  Cleaning up extraneous data... "
-		rm -rf Qt/{aqtinstall.log,Tools}
+		rm -rf aqtinstall.log Tools
 
 		echo Done.
 	fi
diff --git a/apps/openmw/engine.cpp b/apps/openmw/engine.cpp
index fce1ed8..597ea47 100644
--- a/apps/openmw/engine.cpp
+++ b/apps/openmw/engine.cpp
@@ -1178,9 +1178,12 @@ void OMW::Engine::go()
         DrawThreadPerContext (OSG default) — один поток Draw блокирует Cull.
         CullDrawThreadPerContext — Cull и Draw работают параллельно,
         что снижает Draw ms и GPU idle time при одном контексте.
-        Включаем thread-safe счётчики ссылок чтобы избежать гонок.
+
+        Не вызываем osg::Referenced::setThreadSafeReferenceCounting(true):
+        в части сборок/пакетов OSG 3.6.x этот API отсутствует, что ломает
+        Windows CI. Само переключение threading model для Viewer от этого
+        не зависит.
     */
-    osg::Referenced::setThreadSafeReferenceCounting(true);
     mViewer->setThreadingModel(osgViewer::ViewerBase::CullDrawThreadPerContext);
 
 #if OSG_VERSION_GREATER_OR_EQUAL(3,5,5)
